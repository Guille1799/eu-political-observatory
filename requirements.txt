# Declared from what this repository actually imports, not from whatever happened to
# be installed in the author's environment. Versions pinned with `==` are the ones
# verified in the reference environment; pip resolves the transitive dependencies.
#
# Until 2026-08-23 this file was a full `pip freeze`: it shipped torch, transformers,
# fastapi and uvicorn — which no file in this repository imports — while omitting
# three packages the notebooks do import.

# --- Pipeline core (src/, scripts/, eval/) ---
pandas==3.0.2
numpy==2.4.4
psycopg2-binary==2.9.12          # load into PostgreSQL
python-dotenv==1.2.2             # credentials stay out of the code

# --- TLS chain of trust: src/v2/cadena_confianza.py ---
# Verifies the FNMT-RCM intermediate against the certifi roots instead of disabling
# verification. Without `cryptography` that module cannot run at all.
certifi==2026.4.22
cryptography>=42                 # NOT PINNED: absent from the reference environment

# --- Statistical layer used by the notebooks ---
scipy==1.17.1
scikit-learn==1.5.2
factor_analyzer==0.5.1           # EFA (see R/README.md: the model is not settled)
pyreadstat==1.3.4                # reads ESS microdata (.sav)
matplotlib==3.10.9
seaborn==0.13.2

# --- To run the notebooks ---
jupyterlab==4.5.7
ipykernel==7.2.0

# --- Tests ---
pytest==9.0.2
